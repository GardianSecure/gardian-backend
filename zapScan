// zapScan.js
const axios = require("axios");
const ZAP_API = "http://localhost:8080";

async function runZapScan(siteUrl) {
  // Wait until ZAP is ready
  for (let i = 1; i <= 40; i++) {
    try {
      const res = await axios.get(`${ZAP_API}/JSON/core/view/version/`);
      if (res.data) {
        console.log("✅ ZAP daemon is ready");
        break;
      }
    } catch {
      console.log(`⏳ Waiting for ZAP... attempt ${i}/40`);
      await new Promise(r => setTimeout(r, 3000));
    }
    if (i === 40) throw new Error("ZAP daemon did not become ready in time");
  }

  // Start active scan
  const scanRes = await axios.get(
    `${ZAP_API}/JSON/ascan/action/scan/?url=${encodeURIComponent(siteUrl)}`
  );
  const scanId = scanRes.data.scan;
  console.log(`🔍 Scan started with ID: ${scanId}`);

  // Poll until scan completes
  let progress = 0;
  while (progress < 100) {
    const statusRes = await axios.get(
      `${ZAP_API}/JSON/ascan/view/status/?scanId=${scanId}`
    );
    progress = parseInt(statusRes.data.status, 10);
    console.log(`⏳ Scan progress: ${progress}%`);
    await new Promise(r => setTimeout(r, 5000));
  }

  // Fetch alerts
  const alertsRes = await axios.get(
    `${ZAP_API}/JSON/core/view/alerts/?baseurl=${encodeURIComponent(siteUrl)}`
  );
  return alertsRes.data.alerts || [];
}

module.exports = { runZapScan };
